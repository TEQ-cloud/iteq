# Examples

Real-world deployment references that go beyond the chart defaults:

- `cnpg-cluster.yaml` — a production CNPG cluster definition *(to be added)*
- `argo-applicationset.yaml` — GitOps deployment via Argo CD ApplicationSet *(to be added)*

Contributions from actual installations welcome — these are meant to be copied
and adapted, not applied blindly.
